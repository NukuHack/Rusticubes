this folder has the game related stuff
these are stuff what only apply to a single world, meaning they will be freed from memory at world leaving


// game related, instance related
pub mod game {
	// main camera and player impl.
	pub mod player;
	// game-state with seed and stuff
	pub mod state;
}

